<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <%- include('./parts/navbar.ejs') %>
    <div class="container">
      <% pubs.forEach(element => { %>
      <div>
        <h2><%= element.user %></h2>
        <p><%= element.pub %></p>
       <a href="/post/<%= element._id %>"> <button>show more</button></a>
      </div>
      <% }) %>
    </div>
  </body>
</html>
